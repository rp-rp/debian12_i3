alias mocp="mocp -T rp_theme"

alias tty-clock="tty-clock -c -s -B -C 3"

alias wifi="sudo nano /etc/network/interfaces"

alias wifi_ssid="sudo iwlist wlp4s0 scan | grep ESSID"

alias wifi_connected="printf 'Connected to: '; sudo iwgetid -r"
